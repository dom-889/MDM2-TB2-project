right so i think ive figured out how git works now.
id say installing git (the app) is probably the best method as it actually has a gui to use and i still cant get it to work on the command line on mac. although i have managed to do it on windows its way more convinient to use the app so id just do that.
and as for the general file structure and stuff, just make sure youve set this repo to be the one you're using in the project thing just so that when you flag somehting or put a goal or whatever on it actually shows up on this repo and is just more convinient.

i would also reccomend just getting familiar with the whole pushing, pulling, adding, commiting and all that jazz tho caause it is a smidge confusing.

general order that you should follow should be:
- pull (gets the most recent version and makes sure you're not pushing an outdated version)
- add file_name (if youre working from the command line youll need to use this but not if you're on the app iirc)
- commit -m "add comment here" (thats the structure you need to use for the commandline but on the app its just the button i think and also this command overwrites the git repo locally or something (idk what it actually does but yh))
- push (this just uploads the commit to github and then bosh youve changed something (i would highly reccomend trying it out a bit just to get used to it))

another thing to mention is that all of those commands i just listed need to be ran as git <command> and then they work
also maybe get vim if you want cause it makes stuff easier (enormous lie btw) 

icl just get the app its so much easier

basically all you need to do on the app is:
- clone the repo
- pull the repo
- open in vs code (and make your changes)
- pull the repo
- push the changes
- bosh


- dom
