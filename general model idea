create a random heterogenious volume to run an art algorithm on using arrays and a nearest neighbour algorithm (or any appropriate grouping algorithm) to create segments of materials
use a dictionary that corresponds to each of the values (materials) and can be used to check the properties (maybe oop could be better but idk)
the most useful value will be the attentuation coefficient which will be used during the 'mapping' phase of the model, the attenuation value corresponds to how much energy is lost when travelling through it (i think)
after this initial set up is done, the mapping phase can begin:
this involves taking a single array (slice) from the volume and then using this to fire a number of waves of a known energy from a 'beam emmitter' positioned at a fixed distance from the centre of the slice through the slice in a fan-like shape at 'detectors' on the opposite side of the 'circle' that the beam emmitter is also located on.
these waves lose energy as they pass through the different materials, and to model them the following process can be used:
- take a slice of the beam (or a vector from the emmitter to the detector) and slice it up into small chunks (create a tolerance, maybe 10**-3 or -4) - iterate through the beam segments checking what material each node of the segments are in then use this data (perhaps in a list) 
- then calculate the total attenuation of the beam (ill double check what this process is called cause it has a name)
- this can then be used to plot attenuation graphs for each position of the emmittter and also create a sinogram
- this is then processed to create a 2d image of the slice
- repeat this process for each slice and then your algorithm is complete

currently just need to figure out how to do the conversion then i may have a gander at implementation